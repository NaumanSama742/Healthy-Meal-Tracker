<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.admin_panel">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Admin_Panel">

        <activity android:name=".AdminMealsActivity" />
        <activity android:name=".AdminUsersActivity" />
        <activity android:name=".AdminCoachesActivity" />
        <activity android:name=".SuggestedMealsActivity" />
        <activity android:name=".AvailableMealsActivity" />
        <activity android:name=".MealIntakeHistoryActivity" />


        <!-- Existing MainActivity declaration with launcher intent -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>

</manifest>
